import React from "react";

const UserCard = (name ) => {
    return <div>
        <p>{name}</p>
        {/* <p>{surname}</p>
        <p>{email}</p>
        <p>{country}</p>
        <p>{street}</p>
        <p>{postal}</p>
        <p>{city}</p> */}
    </div>
}

export default UserCard